<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">E-Learning</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link #{"active" if current_page?(root_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Assignments", assignments_path, class: "nav-link #{"active" if current_page?(assignments_path)}" %>
        </li>
        
      </ul>
    </div>
     <form class="d-flex">
        <div class="m-2"><%= current_user.email %></div>
            <%= link_to "Logout", logout_path, data: { turbo_method: :delete, confirm: "Are you sure?" }, class: "btn btn-outline-danger" %>
      </form>
  </div>
</nav>
